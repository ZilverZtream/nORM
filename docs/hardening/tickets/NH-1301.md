# NH-1301 - confirm benchmark governance (thresholds deferred)

Ticket: NH-1301
Status: Verified
Owner: unassigned
Area: performance/benchmark-governance
Priority: P2
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~BenchmarkFairnessLock|FullyQualifiedName~BenchmarkGovernance"

## Problem

The performance domain has two separable parts: (a) benchmark **governance** - the methodology,
fairness locks, and baseline rules, which are unit-testable and gate-enforced; and (b) the actual
**threshold pass** - a fresh, isolated benchmark run whose numbers depend on stable hardware. This
ticket confirms ONLY (a). Claiming the thresholds "green" from an in-session run would be
overclaiming (benchmarks are thermally/environment sensitive), so (b) is explicitly deferred.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-1301.md
- docs/v1-sharpening/performance.md

Acceptance criteria:

- [x] The benchmark-governance / fairness-lock unit tests run dry on the current tree.
- [x] Benchmark methodology (`docs/benchmark-governance.md`) - baseline rules, fairness locks,
      fastest-row-per-method rule - is explicit and enforced by those tests.

## Parity audit

- [x] IMPLEMENTATION-DEBT: the actual benchmark THRESHOLD pass (fresh isolated run for every
      provider-matrix method, fastest-method-is-nORM, no hot-path regression vs baseline) is NOT
      verified here. It requires an isolated benchmark run on stable hardware (delete
      `benchmarks/bin` first for SQLITE_FULL) and a re-baseline near the RC. Deferred to RC prep;
      not claimed green. This is a quality bar, never a correctness gate.

## Correctness-first audit

- [x] Honest boundary: only the provable governance part is claimed; the numbers are deferred, not
      hand-waved as green.
- [x] No throw-pin / no docs-only caveat hiding a defect: performance never gates correctness, and
      the deferral is explicit.

## Required verification

- The named gate (governance unit tests) -> green.
- Threshold run: `eng/run-provider-benchmark-slice.ps1` + `eng/check-benchmark-thresholds.ps1` on
  stable hardware - DEFERRED, not run here.

## Reference app impact

None directly; the benchmark matrix runs on the release gate / dedicated hardware.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 15` (benchmark fairness-lock + governance contract).
- Benchmark thresholds: NOT run here - deferred to an isolated bench run (RC prep).
