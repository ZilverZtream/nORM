# NH-1101 - confirm AOT, trimming & source-generation coverage

Ticket: NH-1101
Status: Verified
Owner: unassigned
Area: aot-sourcegen/correctness
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~AotTrimmingPolicy|FullyQualifiedName~SourceGen|FullyQualifiedName~SourceGenerator|FullyQualifiedName~CompiledMaterializer"

## Problem

The AOT/source-generation correctness surface (the empty AOT publish diagnostic baseline, honest
RUC/RDC annotations, and source-generated materializer/query correctness) must be confirmed by a
gate run + the AOT publish scan on the current tree, not assumed - especially after the NH-0501
provider-hook src change.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-1101.md
- docs/v1-sharpening/aot-sourcegen.md

Acceptance criteria:

- [x] The AOT publish scan on the current tree reports **0 IL diagnostics** (baseline stays empty;
      no unannotated dynamic-code leak).
- [x] The source-gen correctness + AOT-trimming-policy slice run dry on the current tree.
- [x] The documented deployment boundary (`docs/aot-trimming.md`) matches reality
      (`AotTrimmingPolicy` green).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the source-gen correctness fuzzer compares source-generated materializers
      against the reflection runtime; the AOT publish scan is the definitive IL-diagnostic proof.
- [x] Honest boundary: the sustained source-gen fuzzer dry window is an open item (calendar time);
      the AOT exit criteria themselves are met and do not depend on live providers.

## Required verification

- `dotnet publish src/nORM.csproj -c Release -r linux-x64 --self-contained -p:PublishAot=true`,
  grep `IL\d{4}` -> 0 (the `GeneratePackageOnBuild is not supported` error at the end is expected).
- The named gate -> all green.

## Reference app impact

`PackageConsumerIntegrationTests` exercises the source generator in the packed nupkg (pack-based,
run on the gate).

## Verification evidence

- AOT publish scan on the current tree -> `IL diagnostics: 0` (after the NH-0501 provider-hook src
  change).
- Named gate -> `Passed! Failed: 0, Passed: 148` (AOT trimming policy, source-gen materializer
  correctness, source generator, compiled materializer).
