# NH-0101 - confirm Query & LINQ correctness coverage

Ticket: NH-0101
Status: Verified
Owner: unassigned
Area: query-linq/correctness
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~LinqParityFuzz|FullyQualifiedName~QueryTranslatorCoverage|FullyQualifiedName~LinqSequenceTailOperator|FullyQualifiedName~LinqWhereAfterTake|FullyQualifiedName~LinqCompositeNavigation|FullyQualifiedName~ProjectionClosureTrailingOperator|FullyQualifiedName~CorrelatedSubqueryGlobalFilter|FullyQualifiedName~LinqExpressionFingerprintStability|FullyQualifiedName~NormQueryProviderCoverage|FullyQualifiedName~LinqWhereDateTimeNowComparison"

## Problem

The LINQ translation correctness surface (the LINQ-parity differential fuzzer vs a
LINQ-to-Objects oracle, plus query-translator coverage and shape regressions) must be confirmed
green by a gate run on the current tree, not assumed. This ticket records that evidence. It does
NOT claim the cross-cutting bars - the sustained multi-week fuzzer-dry window and live
cross-provider parity - which stay open.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0101.md
- docs/v1-sharpening/query-linq.md

Acceptance criteria:

- [x] The LINQ-parity fuzzer + query correctness regression slice run dry on the current tree.
- [x] The parity oracle is differential (seeded query shapes executed vs LINQ-to-Objects), not a
      happy-path assertion.

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the LINQ-parity fuzzer compares nORM's SQL execution against an
      in-memory LINQ-to-Objects oracle over seeded shapes.
- [x] Honest boundary: live cross-provider parity (needs credentials), the `docs/linq-support.md`
      matrix re-sync, and the `NormUnsupportedFeatureException` throw-site audit are NOT claimed
      here and stay open in the domain doc.

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing query/LINQ correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 351` (LINQ-parity fuzzer + query-translator coverage,
  sequence-tail operators, Where-after-Take, composite navigation, projection-closure trailing
  operators, correlated-subquery global filter, expression-fingerprint stability, query-provider
  coverage, DateTime.Now comparisons).
- Sustained multi-week dry window and live cross-provider parity: NOT claimed (cross-cutting bar).
