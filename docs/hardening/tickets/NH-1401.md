# NH-1401 - confirm security & data-protection coverage

Ticket: NH-1401
Status: Verified
Owner: unassigned
Area: security/data-protection
Priority: P0
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~RawSqlSecurity|FullyQualifiedName~StoredProcedureSecurity|FullyQualifiedName~Redaction|FullyQualifiedName~ProviderSecurityMatrix|FullyQualifiedName~RawSqlBoundary|FullyQualifiedName~MigrationMetadataEscaping|FullyQualifiedName~ParameterMetadataContamination"

## Problem

The security surface (safe parameterisation / no concatenation, strict identifier validation,
raw-SQL and stored-procedure safety + strict-mobility blocking, log redaction, and tenant
isolation as a security boundary) must be confirmed by a gate run, not assumed. Security bars are
absolute - an injection path or redaction leak is a defect, not a limitation.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-1401.md
- docs/v1-sharpening/security.md

Acceptance criteria:

- [x] The security slice runs dry: safe parameterisation, strict identifier validation, raw-SQL +
      stored-procedure safety and doc contract, redaction, provider security matrix, metadata
      escaping, and parameter-metadata contamination.
- [x] Parameterisation is safe by construction (no user value concatenated into SQL); raw-SQL and
      stored-procedure surfaces are documented, blocked under strict provider mobility, and
      injection-resistant.
- [x] Tenant isolation (the security boundary) is confirmed (cross-ref NH-0701: 157-test
      cross-tenant adversarial sweep green) and log redaction never leaks (`Redaction` green).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Injection resistance is proven by construction: parameters are bound, never concatenated;
      identifiers are validated by strict regex. The tests assert unsafe inputs are rejected/bound.
- [x] Honest boundary: live native-RLS on the three servers is deferred (NH-0701 / live gate);
      live DB credentials stay in `NORM_TEST_*` env vars, test-only, never committed.

## Required verification

- The named gate -> all green.

## Reference app impact

Tenant + security flows are exercised by `samples/nORM.Sample.Store`; live native-RLS on the live
provider gate.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 218` (raw-SQL security + boundary, stored-procedure
  security, redaction, provider security matrix, migration metadata escaping, parameter-metadata
  contamination).
- Tenant isolation: cross-ref NH-0701 (157 tests). Live native-RLS: NOT claimed (live gate).
