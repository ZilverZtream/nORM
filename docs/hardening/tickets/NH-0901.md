# NH-0901 - confirm resilience & concurrency correctness coverage

Ticket: NH-0901
Status: Verified
Owner: unassigned
Area: resilience/concurrency
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~OccInterleavingFuzz|FullyQualifiedName~MixedTokenOccFuzz|FullyQualifiedName~ConcurrencyToken|FullyQualifiedName~ConcurrencyRowcountSemantics|FullyQualifiedName~OptimisticConcurrencyLostUpdate|FullyQualifiedName~OccConflictMatrixAffectedRows|FullyQualifiedName~ConcurrentWrite|FullyQualifiedName~OccConflictMatrix"

## Problem

The resilience/concurrency correctness surface (OCC interleaving + mixed-token fuzzers,
concurrency-token semantics, rowcount semantics, lost-update prevention, conflict matrix,
concurrent writes, and retry-write invariants under fault injection) must be confirmed green by a
gate run, not assumed. This ticket records that evidence. Live PostgreSQL/MySQL OCC matrix and the
live SQL Server deadlock path stay open (live provider gate).

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0901.md
- docs/v1-sharpening/resilience.md

Acceptance criteria:

- [x] The OCC interleaving + mixed-token fuzzers, concurrency-token/rowcount-semantics,
      lost-update, conflict-matrix, and concurrent-write slice run dry on the current tree.
- [x] No lost updates under the fuzzed interleavings; the client-managed 8-byte token is correct
      across write shapes (`OptimisticConcurrencyLostUpdate`, `OccConflictMatrix*` green).
- [x] Retry-write invariants hold under fault injection (`SaveChangesFaultInjectionAtomicity`
      green; cross-ref NH-0201): db-generated keys reset on retried SaveChanges; never retried
      past commit-attempted.

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the OCC interleaving fuzzer runs concurrent writer schedules against an
      oracle that treats value-unchanged mutations as non-writes (no false conflicts).
- [x] Honest boundary: live PostgreSQL/MySQL OCC matrix, the live SQL Server deadlock-resilient
      path (error 1205), and the sustained dry window are NOT claimed here.

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing concurrency correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 70` (OCC interleaving fuzzer, mixed-token OCC fuzz,
  concurrency-token, rowcount semantics, lost-update prevention, conflict matrix + affected-rows,
  concurrent writes).
- Live PostgreSQL/MySQL OCC matrix, live SQL Server deadlock path, and the sustained multi-week
  dry window: NOT claimed (cross-cutting bar).
