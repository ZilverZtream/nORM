# NH-1001 - confirm scaffolding & CLI tooling coverage

Ticket: NH-1001
Status: Verified
Owner: unassigned
Area: scaffolding/compile-checks
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~DatabaseScaffolder|FullyQualifiedName~ScaffoldingContractDoc"

## Problem

The scaffolding correctness surface (in-proc Roslyn compile checks of generated code, safety
modes, and the doc-to-code scaffolding contract) must be confirmed green by a gate run, not
assumed. This ticket records that evidence. Live-provider schema parity across the four servers,
database-drop safety on live providers, and the CLI-surface freeze stay open.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-1001.md
- docs/v1-sharpening/scaffolding-cli.md

Acceptance criteria:

- [x] The in-proc scaffolder compile-check + scaffolding-contract-doc slice run dry on the current
      tree.
- [x] Scaffolded output compiles across supported shapes (with and without relationships); safety
      modes (`OverwriteFiles`/`DryRun`/`FailOnWarnings`/keyless handling) are covered.
- [x] The scaffolding doc-to-code contract holds and every `ScaffoldOptions` member is tested
      (`ScaffoldingContractDoc` green; members recorded in `docs/public-api-policy.md`).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the compile-check tests actually compile the generated code via in-proc
      Roslyn - "it compiles" is proven, not asserted.
- [x] Honest boundary: live-provider schema parity, database-drop safety on live providers, and
      the CLI command-surface freeze are NOT claimed here.

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing scaffolding correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 798` (DatabaseScaffolder basic/output/relationship/
  many-to-many/keyless-safety and the scaffolding contract-doc suites; in-proc Roslyn compile
  checks).
- Live-provider schema parity, live database-drop safety, and the CLI-surface freeze: NOT claimed.
