# NH-0401 - confirm schema & migrations correctness coverage

Ticket: NH-0401
Status: Verified
Owner: unassigned
Area: migrations/correctness
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~MigrationDataPreservation|FullyQualifiedName~MigrationRename|FullyQualifiedName~SchemaSnapshot|FullyQualifiedName~SchemaSignature|FullyQualifiedName~MigrationIdentityGuard|FullyQualifiedName~MigrationSqlGeneration|FullyQualifiedName~MigrationSnapshotFluent|FullyQualifiedName~MigrationMetadataEscaping|FullyQualifiedName~RuntimeSnapshotFk"

## Problem

The schema/migrations correctness surface (up/down data-preservation fuzzer, rename detection,
SQLite table-recreate, schema snapshot/signature, identity guard, and provider DDL generation)
must be confirmed green by a gate run, not assumed. This ticket records that evidence. Live-DB
DDL round-trip, per-provider concurrent-deploy advisory locking, and the sustained dry window
stay open (cross-cutting bar).

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0401.md
- docs/v1-sharpening/migrations.md

Acceptance criteria:

- [x] The data-preservation fuzzer + rename / schema-snapshot / schema-signature / DDL-generation
      slice run dry on the current tree.
- [x] Down-runnable-standalone with dropped-NOT-NULL restore, the SQLite one-recreate + rename-fold
      invariant, and provider DDL precision (SQL-generation level) are covered and green.
- [x] Rename detection matches `[RenameColumn]` (doc contract green).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the data-preservation fuzzer is an up/down oracle over random schemas +
      data; DDL generation is asserted at the SQL-string level per provider.
- [x] Honest boundary: live-DB DDL round-trip and per-provider concurrent-deploy advisory locking
      (need credentials) and the sustained dry window are NOT claimed here.

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing migration correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 220` (data-preservation fuzzer, rename detection +
  doc contract, schema snapshot/signature, identity guard, provider DDL generation, fluent
  snapshot, metadata escaping, runtime snapshot FKs).
- Live-DB DDL round-trip, live advisory locking, and the sustained multi-week dry window: NOT
  claimed (cross-cutting bar).
