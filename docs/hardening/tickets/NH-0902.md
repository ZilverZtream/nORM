# NH-0902 - Resilience per-cell matrix (deep)

Ticket: NH-0902
Status: In Progress
Owner: unassigned
Area: resilience/retry-interplay
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~RetryCache|FullyQualifiedName~RetryRead|FullyQualifiedName~TimeoutContract" (extended per cell as cells close)

## Problem

NH-0901 closed the retry-WRITE invariants (never past commit-attempted, db-generated key reset -
`SaveChangesFaultInjectionAtomicity`) and the OCC matrix. What no suite pins per-cell: the
INTERPLAY of the retry strategy with the read pipeline and the result cache
(`RetryingExecutionStrategy` wraps `ExecuteInternalAsync` INCLUDING the cached path - a transient
failure inside the cache factory must retry cleanly, never double-Set or serve a partial result),
read-retry idempotence, fail-loud connection-drop contracts on reads, and per-plan timeout
honoring. This matrix closes those cells with fault-injection probes.

## Non-negotiable scope

Allowed paths:

- tests/**
- src/nORM/Query/**
- src/nORM/Core/**
- src/nORM/Execution/**
- src/nORM/Providers/**
- docs/hardening/tickets/NH-0902.md
- docs/v1-sharpening/resilience.md

Acceptance criteria:

`[x]` closed, `[~]` partial/live-pending, `[ ]` open.

- [x] RETRY x RESULT CACHE -> TWO IMPLEMENTATION-DEBT KILLS FOUND AND FIXED: (1) the read pipeline wraps provider exceptions in NormException before they reach RetryingExecutionStrategy, whose catch filter tested the WRAPPER type - a transient DbException surfacing from any read escaped with ZERO retries, making RetryPolicy dead code on reads; the strategy now classifies (filter + ShouldRetry) by the unwrapped innermost provider exception. (2) the fast path executed OUTSIDE the retry strategy entirely, so the most common query shapes bypassed the policy; the fast path now bails to the full pipeline when a RetryPolicy is configured. Pinned by ResilienceRetryCacheContractTests: transient first attempt retries and populates the cache EXACTLY once, exhausted retries surface loudly with nothing cached, no-policy failures stay loud. PROBE LESSON: SQLite prefers sync reader execution - fault injectors must override BOTH ReaderExecuting hook variants or the injection is silently inert. Original scope: with a RetryPolicy configured, a Cacheable read whose FIRST attempt
      fails transiently must succeed on retry with correct data, populate the cache exactly once,
      and later hits serve the correct entry; a transient failure must never cache a partial or
      empty result. PROBE with a fault-injecting command interceptor.
- [x] READ RETRY IDEMPOTENCE -> CORRECT after the fixes above and PINNED (retried tracked read yields one tracked instance per row; aggregates correct on retried Count). Original scope: a transient failure mid-read retries to a correct, non-duplicated
      result set (tracked entities not double-tracked; aggregates correct).
- [ ] CONNECTION-DROP FAIL-LOUD -> a dropped/broken connection mid-read without a retry policy
      surfaces a loud provider exception, never a silent partial list.
- [ ] TIMEOUT CONTRACTS -> plan CommandTimeout / options BaseTimeout reach the command for reads,
      CUD, and bulk paths (pin the wiring; a sub-second-timeout long query throws timeout rather
      than hanging - SQLite-closable via interceptor observation of CommandTimeout).
- [x] WRITE RETRY INVARIANTS -> genuinely covered by NH-0901's `SaveChangesFaultInjectionAtomicity`
      (never retry past commit-attempted; db-generated keys reset on retried SaveChanges) - cited,
      not duplicated.

## Parity audit

- [x] This ticket introduces no new restriction; it pins retry/timeout interplay contracts.

## Correctness-first audit

- [ ] Each cell probed with real fault injection before it is marked.

## Required verification

- The named gate (grows per cell) -> green.

## Verification evidence

- (accumulates per cell)
