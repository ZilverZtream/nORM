# NH-0601 - confirm temporal & versioning correctness coverage

Ticket: NH-0601
Status: Verified
Owner: unassigned
Area: temporal/correctness
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~TemporalHistoryReconstruction|FullyQualifiedName~TemporalAsOfPlanCache|FullyQualifiedName~TemporalLifecycleHardening|FullyQualifiedName~TemporalPrecision|FullyQualifiedName~TemporalTriggerTenantScope|FullyQualifiedName~TemporalProjectionAndTemporalTracking|FullyQualifiedName~ProviderNativeTemporal|FullyQualifiedName~TenantTemporalProviderSwap"

## Problem

The temporal correctness surface (history reconstruction fuzzer, `AsOf` cross-plan re-binding,
sub-second precision, provider-native vs nORM-managed storage gating, and temporal+tenant
interactions) must be confirmed green by a gate run, not assumed. This ticket records that
evidence. All-four-provider sub-second precision and the sustained dry window stay open.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0601.md
- docs/v1-sharpening/temporal.md

Acceptance criteria:

- [x] The reconstruction fuzzer + AsOf plan-cache + precision + provider-native + tenant-scope
      temporal slice run dry on the current tree.
- [x] `AsOf` re-binds across cached plans and strict provider mobility gates provider-native
      temporal storage as provider-bound (`TemporalAsOfPlanCache`, `ProviderNativeTemporal` green).
- [x] SQLite ms-precision `AsOf` is correct (`TemporalPrecision` green).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the reconstruction fuzzer replays history and compares against the
      expected version chain; precision tests use gaps larger than clock granularity.
- [x] Honest boundary: sub-second precision on all four providers' triggers, temporal+soft-delete
      interaction, and the sustained dry window are NOT claimed here.

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing temporal correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 50` (history reconstruction fuzzer, AsOf plan-cache
  re-binding, lifecycle hardening, ms precision, trigger tenant scope, projection/temporal
  tracking, provider-native temporal, tenant+temporal provider swap).
- All-four-provider sub-second precision and the sustained multi-week dry window: NOT claimed.
