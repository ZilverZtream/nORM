# NH-0502 - Provider-mobility per-cell matrix (deep)

Ticket: NH-0502
Status: In Progress
Owner: unassigned
Area: provider-mobility/hook-parity
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~ProviderHookOverrideMatrix" (extended per cell as cells close)

## Problem

nORM's provider mobility rests on the DatabaseProvider hook system: every provider-specific SQL
divergence must flow through a virtual hook, and each provider's override SET is the executable
statement of where it diverges from the portable default. Nothing currently pins that override set -
a new hook added without per-provider consideration, or an override added/removed casually, changes
cross-provider semantics silently. This ticket builds the per-cell provider-mobility matrix:
an exhaustive hook-override matrix contract (SQLite-closable), DDL type-mapping parity, DML shape
parity, and live-pending behavioural probes for the divergences recorded by earlier matrices
(SqlServer uniqueidentifier ordering, MySQL native unsigned-64).

## Non-negotiable scope

Allowed paths:

- tests/**
- src/nORM/Providers/**
- src/nORM/Query/**
- docs/hardening/tickets/NH-0502.md
- docs/v1-sharpening/provider-mobility.md

Acceptance criteria:

`[x]` closed, `[~]` partial/live-pending, `[ ]` open.

- [ ] HOOK OVERRIDE MATRIX (SQLite-closable) -> enumerate every overridable SQL-shaping hook on
      DatabaseProvider (reflection over virtual members) and assert per provider that its DECLARED
      override set matches the documented divergence profile exactly - a hook override added or
      removed on any provider without updating the matrix fails the test, and a NEW virtual hook not
      yet classified in the matrix fails the test.
- [ ] DDL TYPE-MAPPING PARITY (SQLite-closable) -> every generator maps the shared CLR type-family
      set (the NH-0202 families) to its documented provider type; extend existing SqlGenerator tests
      only where a family is missing.
- [ ] DML SHAPE PARITY (SQLite-closable) -> parameter prefixes, identifier escaping, and paging
      clause forms per provider; cite existing coverage (ProviderCoverageTests etc.) and extend only
      gaps.
- [ ] LIVE-PENDING: SqlServer uniqueidentifier ORDER BY divergence probe (recorded by the NH-0102
      Guid cell: .NET Guid.CompareTo vs uniqueidentifier field order) - requires live SqlServer.
- [ ] LIVE-PENDING: MySQL native BIGINT UNSIGNED behaviour vs the portable signed-64 ulong contract -
      requires live MySQL.
- [ ] LIVE-PENDING: the standing live translation-parity gate (LinqParityFuzzLiveTests +
      LiveProvider* suites) re-run green on all three servers after this campaign's translation
      changes (NORM_DECIMAL collation and decimal aggregates are SQLite-only; the hook defaults on
      server providers are identity - verify no drift).

## Parity audit

- [x] This ticket introduces no new restriction; it pins the existing divergence profile so future
      changes to it are deliberate.

## Correctness-first audit

- [ ] Each SQLite-closable cell to be probed/asserted before it is marked; live-pending cells stay
      [~] until the live suites run.

## Required verification

- The named gate (grows per cell) -> green.

## Verification evidence

- (accumulates per cell)
