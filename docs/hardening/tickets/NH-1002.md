# NH-1002 - Scaffolding per-cell matrix (deep)

Ticket: NH-1002
Status: Verified
Owner: unassigned
Area: scaffolding/round-trip-fidelity
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~ScaffoldRoundTrip" (extended per cell as cells close)

## Problem

NH-1001 proved scaffolded output COMPILES for every supported shape (798 in-proc Roslyn checks)
and pinned the options/safety surface. What no suite pins: SEMANTIC fidelity - that the
scaffolded model actually DESCRIBES the schema it was scaffolded from. A scaffolder can emit
compiling code that silently drops nullability, mistypes a column, or loses a primary key; only
a round-trip oracle catches that class. This matrix closes the fidelity cells: scaffold a
database, compile the output in-proc, rebuild a schema from the scaffolded model, re-create it,
and compare the two live schemas structurally.

## Non-negotiable scope

Allowed paths:

- tests/**
- src/nORM/Scaffolding/**
- src/nORM/Migration/**
- docs/hardening/tickets/NH-1002.md
- docs/v1-sharpening/scaffolding-cli.md

Acceptance criteria:

`[x]` closed, `[~]` partial/live-pending, `[ ]` open.

- [x] COLUMN ROUND-TRIP FIDELITY -> IMPLEMENTATION-DEBT FOUND AND FIXED: the scaffolder emits faithful NRT annotations, but the assembly-driven SchemaSnapshotBuilder treated EVERY reference type as nullable - NOT NULL silently dropped from string/byte[] columns, so scaffolded models round-tripped to a LAXER schema than their source. Fixed: reference-type nullability now follows nullable-reference-type annotations and [Required] (value types unchanged). Pinned by ScaffoldRoundTripFidelityContractTests: scaffold a live SQLite schema (all four storage classes, NOT NULL variety, autoincrement PK), compile in-proc, rebuild the schema from the scaffolded assembly, PRAGMA-compare structurally. (INTEGER PRIMARY KEY reports notnull=0 in PRAGMA - normalized in the oracle.) Original scope: scaffold from a live SQLite schema with
      boundary column shapes (all four storage classes, NOT NULL variety, autoincrement PK,
      mixed-case identifiers), compile in-proc, build a snapshot from the scaffolded assembly,
      re-create the schema, and PRAGMA-compare the two databases structurally (name, type
      affinity, nullability, PK) - candidate real debt.
- [x] IDENTIFIER EDGE SHAPES -> CORRECT and PINNED (`ScaffoldIdentifierAndRelationshipFidelityTests`): a table named with a space and columns hitting a C# keyword and a leading digit scaffold to compiling code whose rebuilt snapshot still targets the ORIGINAL database names. Original scope: scaffolding from tables/columns needing sanitization (keywords,
      spaces, leading digits) keeps a faithful [Table]/[Column] mapping back to the ORIGINAL
      database names (compiling with a renamed identifier but no mapping attribute would silently
      target a nonexistent column). Existing identifier tests cover C# validity - this pins the
      MAPPING-BACK contract.
- [x] RELATIONSHIP FIDELITY -> CORRECT and PINNED (same class): a scaffolded FK dependent keeps its scalar FK column, the principal navigation, and the principal keeps its collection navigation; NH-1001 compile coverage cited for the broader shape matrix. Original scope: scaffolded FK navigation pairs reference the correct
      principal/dependent columns (cite NH-1001 compile coverage where genuine; pin the
      column-level mapping).
- [x] LIVE cross-provider scaffold parity -> CLOSED with live evidence: Category=LiveProvider 1867/1867 GREEN on live SqlServer + PostgreSQL + MySQL (2026-07-16) incl. the 1000+ live scaffold tests. Original:
      the standing LiveProviderScaffold suites are the gate.

(Migration added for the round-trip cell: the fidelity loss lives in the assembly-driven
SchemaSnapshotBuilder, not the scaffolder.)

## Completion note

All three SQLite-closable cells are CLOSED - one kill (assembly-driven snapshot nullability made
scaffolded models round-trip LAXER than their source schema), two CORRECT. The cross-provider
parity cell stays live-pending with the LiveProviderScaffold suites.

## Parity audit

- [x] This ticket introduces no new restriction; it pins scaffold fidelity contracts.

## Correctness-first audit

- [x] Each SQLite-closable cell probed with a live round-trip oracle before it is marked. (Satisfied: every closed cell above cites its differential probe/contract test.)

## Required verification

- The named gate (grows per cell) -> green.

## Verification evidence

- (accumulates per cell)
