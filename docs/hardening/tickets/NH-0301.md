# NH-0301 - confirm bulk-operations correctness coverage

Ticket: NH-0301
Status: Verified
Owner: unassigned
Area: bulk/correctness
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~BulkCudOracleFuzz|FullyQualifiedName~BulkProviderParity|FullyQualifiedName~BulkTenantIsolation|FullyQualifiedName~BulkCacheInvalidation|FullyQualifiedName~BulkOccParity|FullyQualifiedName~BatchedBulkFallbackParity|FullyQualifiedName~AdversarialBulkTenant"

## Problem

The bulk-operations correctness surface (bulk-CUD oracle fuzzer, provider parity, fallback
parity, OCC parity, tenant isolation, and cache invalidation on bulk writes) must be confirmed
green by a gate run, not assumed. This ticket records that evidence. Native bulk paths on the
three live servers and the sustained multi-week dry window stay open (cross-cutting bar).

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0301.md
- docs/v1-sharpening/bulk.md

Acceptance criteria:

- [x] The bulk-CUD oracle fuzzer + bulk parity/OCC/fallback/tenant/cache slice run dry on the
      current tree.
- [x] Bulk writes honour tenant boundaries and invalidate cache tags (`BulkTenantIsolation`,
      `AdversarialBulkTenant`, `BulkCacheInvalidation` green).
- [x] Batched/fallback bulk paths match on the local (SQLite) provider (`BatchedBulkFallbackParity`,
      `BulkOccParity` green).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: `BulkCudOracleFuzz` compares bulk results against an oracle; the parity
      suites diff native vs fallback shapes.
- [x] Honest boundary: native bulk paths on live SQL Server / PostgreSQL / MySQL and the sustained
      dry window are NOT claimed here (live provider gate / cross-cutting bar).

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing bulk correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 126` (bulk-CUD oracle fuzzer, bulk provider parity,
  tenant isolation + adversarial tenant, cache invalidation, OCC parity, batched/fallback parity).
- Native bulk paths on live servers + sustained multi-week dry window: NOT claimed (cross-cutting).
