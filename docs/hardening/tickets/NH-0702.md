# NH-0702 - Multi-tenancy per-cell matrix (deep)

Ticket: NH-0702
Status: In Progress
Owner: unassigned
Area: multi-tenancy/isolation
Priority: P0
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~TenantIsolation|FullyQualifiedName~AdversarialTenant" (extended per cell as cells close)

## Problem

Tenant isolation rests on an expression-rewrite filter (root queries and correlated subqueries),
a fail-closed RequireTenantColumn contract, write-path validation, tenant-scoped includes/M2M/
split-query child loads, temporal trigger tenant guards, and tenant-aware plan/result cache keys.
Substantial adversarial coverage exists (AdversarialTenantFuzzTests, AdversarialTenantObjectMatrix,
AdversarialPerfAndTenantIsolation: fast-path bypass, compiled-plan isolation, count-cache scoping,
tenant-id object matrix). What no suite pins per-cell: the INTERPLAY corners - AsOf temporal reads
against the history table, Cacheable result reuse across tenants, bulk CUD (ExecuteUpdate/Delete)
scoping, boundary-valued tenant ids across query shapes, and the fast-path bail-out as an
executable CONTRACT rather than an implementation accident. This matrix closes those cells with
the established differential method (two tenants, shared table, boundary data; assert BOTH
no-cross-tenant-leak AND no-own-row-loss).

## Non-negotiable scope

Allowed paths:

- tests/**
- src/nORM/Query/**
- src/nORM/Core/**
- src/nORM/Providers/**
- docs/hardening/tickets/NH-0702.md
- docs/v1-sharpening/multi-tenancy.md

Acceptance criteria:

`[x]` closed, `[~]` partial/live-pending, `[ ]` open.

- [ ] TEMPORAL AsOf x tenant -> the AsOf FROM-clause rewrite reads `<T>_History` through a
      subquery; PROBE that the tenant filter still applies over it (two tenants, same PK,
      interleaved versions: AsOf must reconstruct ONLY the caller's tenant's version at every
      checkpoint, and tags/current reads must stay scoped). Candidate real debt.
- [ ] CACHEABLE RESULTS x tenant -> a `.Cacheable()` result produced under tenant A must never be
      served to tenant B (and invalidation by writes stays per-table across tenants). PROBE the
      result-cache key path; candidate real debt.
- [ ] QUERY-SHAPE SWEEP -> tenant filter present on every read shape against a two-tenant table:
      aggregates (Count/Sum/Min/Max/Average), GroupBy, joins (incl. join keys on tenant-mixed
      tables), set-ops, window functions, paging tails, First/Single family, Any/All/Contains
      subqueries (SubqueryFilters covers the rewrite - pin behaviourally), and the FAST-PATH BAIL
      CONTRACT: TryExecute/TryExecuteList must refuse tenant-scoped contexts (pin so a future
      fast-path change cannot silently leak).
- [ ] BULK CUD x tenant -> ExecuteUpdateAsync/ExecuteDeleteAsync and BulkInsert/Update/Delete
      only touch the caller's tenant's rows; writes with wrong/null tenant ids keep failing
      closed (ValidateTenantContext contract - cite existing coverage where genuine).
- [ ] BOUNDARY TENANT IDS -> the object-matrix suite covers exotic id shapes; pin the remaining
      numeric/Guid boundary semantics through the read path (0, negative, Guid.Empty as LEGITIMATE
      distinct tenants - not sentinel-confused with "no tenant").
- [ ] LIVE-PENDING: native tenant enforcement parity (Postgres RLS policies, SqlServer/MySql
      session-context) against the portable filter behavior - requires live servers.

## Parity audit

- [x] This ticket introduces no new restriction; it pins the existing fail-closed tenant contract
      and isolation behavior.

## Correctness-first audit

- [ ] Each SQLite-closable cell probed differentially (two tenants, boundary data, leak + loss
      assertions) before it is marked.

## Required verification

- The named gate (grows per cell) -> green.

## Verification evidence

- (accumulates per cell)
