# NH-0201 - confirm write-path correctness coverage

Ticket: NH-0201
Status: Verified
Owner: unassigned
Area: write-path/correctness
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~CrudStateMachineFuzz|FullyQualifiedName~CascadeDeleteReparentedChild|FullyQualifiedName~RequiredReferenceNavClear|FullyQualifiedName~ManyToManyWriteFuzz|FullyQualifiedName~ReferenceNavigationWrite|FullyQualifiedName~ReferenceNavigationSaveChanges|FullyQualifiedName~CollectionNavigationRemovalWrite|FullyQualifiedName~SaveChangesGraphOrder|FullyQualifiedName~DeletedChildNavigationCleanup|FullyQualifiedName~DeletedPrincipalNavigationCleanup|FullyQualifiedName~FkEditNavigationReconciliation|FullyQualifiedName~SaveChangesFaultInjectionAtomicity"

## Problem

The write-path correctness surface (CRUD state-machine oracle fuzzer plus the cascade,
relationship-fixup, graph-order, and fault-injection regressions) must be confirmed green **by a
gate run** on the current tree, not assumed. This ticket records that evidence and marks the
write-path domain's no-silent-data-loss / cascade / fixup criteria as met NOW. It deliberately
does **not** claim the cross-cutting "dry for a sustained window (weeks)" bar - that is calendar
time, tracked at the domain and RELEASE.md level, and stays open.

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0201.md
- docs/v1-sharpening/write-path.md

Acceptance criteria:

- [x] The write-path oracle + regression slice runs dry on the current tree.
- [x] No-silent-data-loss shapes are covered and green: delete-then-re-add, detached-update,
      nav-retarget/reparent cascade, required-nav clear, tracked-dependent cascade, direct+bulk
      tracker sync (the CRUD state machine + KILL 40/41/42 regressions).
- [x] Retry-write invariants under fault injection are green (`SaveChangesFaultInjectionAtomicity`).

## Parity audit

- [x] INTERNAL-INVARIANT: confirmation ticket - no product change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the CRUD state machine is an oracle fuzzer (SQLite + oracle model), not
      happy-path assertions; the cascade/relationship suites are targeted regressions of real kills.
- [x] Honest boundary: the "sustained dry window" bar is NOT claimed here (needs weeks of active
      dev); only the current-tree dry state is recorded.

## Required verification

- The named gate -> all green.

## Reference app impact

None directly; confirms existing write-path correctness.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 59` (CRUD state-machine fuzzer + cascade/reparent,
  required-nav-clear, M2M write fuzz, reference/collection navigation writes, SaveChanges graph
  order, deleted-child/principal cleanup, FK-edit reconciliation, fault-injection atomicity).
- Sustained multi-week dry window: NOT claimed - cross-cutting bar, tracked in RELEASE.md.
