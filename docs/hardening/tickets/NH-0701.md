# NH-0701 - confirm cross-tenant isolation adversarial coverage

Ticket: NH-0701
Status: Verified
Owner: unassigned
Area: multi-tenancy/cross-tenant-isolation
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~AdversarialTenant|FullyQualifiedName~TenantIsolationStress|FullyQualifiedName~IncludeTenantIsolation|FullyQualifiedName~BulkTenantIsolation|FullyQualifiedName~M2MTenantIsolation|FullyQualifiedName~MultiTenantResultCachePoisoning|FullyQualifiedName~MultiTenantPlanCache|FullyQualifiedName~TenantWriteIsolation|FullyQualifiedName~TenantFailClosed|FullyQualifiedName~GlobalFilterEagerLoadLeak|FullyQualifiedName~CompiledQueryTenantIsolation|FullyQualifiedName~OwnedCollectionTenantIsolation"

## Problem

The multi-tenancy domain exit criterion "an adversarial cross-tenant leak sweep finds zero
leaks" must be **confirmed by a gate run**, not assumed from a file listing (no-overclaim). This
ticket records the evidence that cross-tenant isolation is adversarially covered across every
access vector, and marks the corresponding `docs/v1-sharpening/multi-tenancy.md` items closed.
Live native-RLS on the three servers is deliberately **not** claimed here - it belongs to the
live provider gate (cross-cutting bar).

## Non-negotiable scope

Allowed paths:

- docs/hardening/tickets/NH-0701.md
- docs/v1-sharpening/multi-tenancy.md

Acceptance criteria:

- [x] Cross-tenant adversarial coverage exists across reads, writes, bulk, Include/navigation,
      many-to-many, owned collections, compiled queries, result-cache and plan-cache poisoning,
      fail-closed, and global-filter bypass (suites enumerated in the evidence).
- [x] The core cross-tenant adversarial/isolation slice runs green.
- [x] Cache-key tenant-discriminator isolation is confirmed (result-cache poisoning + plan-cache
      suites green), closing the Domain 8 cross-reference for the tenant dimension.

## Parity audit

- [x] INTERNAL-INVARIANT: assessment ticket - no product code change, no restriction introduced.

## Correctness-first audit

- [x] Differential proof: the confirmed suites are adversarial/differential (they assert tenant A
      cannot read or mutate tenant B's rows across each vector), not happy-path assertions.
- [x] No throw-pin or docs-only caveat: this ticket closes only what a gate run proves; live
      native-RLS remains open (deferred to the live provider gate), not overclaimed.

## Required verification

- The named gate (core cross-tenant adversarial/isolation slice) -> all green.

## Reference app impact

`samples/nORM.Sample.Store` exercises tenant + temporal flows; live native-RLS confirmation on
SQL Server / PostgreSQL / MySQL is deferred to the live provider gate.

## Verification evidence

- Named gate -> `Passed! Failed: 0, Passed: 157` (SQLite/in-memory, Fast category), covering:
  `AdversarialTenant{Fuzz,Expression,Object,Navigation}`, `TenantIsolationStress`,
  `{Include,Bulk,M2M,OwnedCollection,CompiledQuery}TenantIsolation`,
  `MultiTenantResultCachePoisoning`, `MultiTenantPlanCache`, `TenantWriteIsolation`,
  `TenantFailClosed`, `GlobalFilterEagerLoadLeak`.
- Live native-RLS (SqlServer/PostgreSQL/MySQL) NOT run here - requires live credentials; tracked
  by the cross-cutting live provider gate.
