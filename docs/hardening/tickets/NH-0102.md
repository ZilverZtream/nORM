# NH-0102 - Query/LINQ per-cell matrix (deep)

Ticket: NH-0102
Status: In Progress
Owner: unassigned
Area: query-linq/operand-matrix
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~StringOrderingContractTests|FullyQualifiedName~GuidOrderingContractTests|FullyQualifiedName~DoubleOrderingContractTests|FullyQualifiedName~NullableOrderingContractTests|FullyQualifiedName~IntegerOrderingContractTests|FullyQualifiedName~DecimalOrderingContractTests|FullyQualifiedName~DateTimeOrderingContractTests|FullyQualifiedName~ScalarTypeOrderingContractTests|FullyQualifiedName~ConverterColumnOrderingContractTests|FullyQualifiedName~AggregateEdgeSemanticsContractTests|FullyQualifiedName~DecimalAggregatePrecisionContractTests" (extended per cell as cells close)

## Problem

NH-0101 confirmed the *existing* LINQ suite is green but did NOT enumerate the operator x
operand-type x edge-case matrix cell by cell. The very first cell probed differentially - string
`OrderBy` vs a .NET oracle - immediately surfaced a real, previously-undocumented divergence
(SQLite ordinal collation vs .NET CurrentCulture). This ticket is the granular Query/LINQ matrix,
worked one cell at a time: probe differentially, classify (IMPLEMENTATION-DEBT / DESIGN-EXCEPTION /
CORRECT-REJECT), fix or pin+document, mark the cell. It stays In Progress until every cell closes.

## Non-negotiable scope

Allowed paths:

- tests/**
- src/nORM/Query/**
- src/nORM/Providers/**
- docs/linq-support.md
- docs/hardening/tickets/NH-0102.md
- docs/v1-sharpening/query-linq.md

(src/nORM/Providers/** is in scope because provider-mobile query translation must route
provider-specific SQL through DatabaseProvider hooks - a query-behaviour fix frequently
adds or overrides a provider hook, which lives under Providers.)

Acceptance criteria:

Each cell = one operator x one operand type x its edge cases. A cell closes only with a committed
differential test vs a LINQ-to-Objects oracle; a divergence is classified and fixed or
pinned+documented, never left silent. `[x]` closed, `[~]` partial, `[ ]` open.

OrderBy / ThenBy x operand type:

- [x] string -> follows DATABASE COLLATION, not .NET CurrentCulture (SQLite BINARY/ordinal).
      DESIGN-EXCEPTION (identical to EF Core). Pinned by `StringOrderingContractTests`, documented
      in `docs/linq-support.md`.
- [x] Guid -> CONSISTENT with .NET `Guid.CompareTo` on SQLite. nORM stores the canonical lowercase
      hex string; SQLite BINARY-orders that text ordinally, which equals field-wise unsigned order
      (what `Guid.CompareTo` does) for every Guid. CORRECT - no code change; pinned by
      `GuidOrderingContractTests` with adversarial high-bit values (a BLOB/byte storage would diverge).
      SQL Server `uniqueidentifier` field-order is a provider-native divergence deferred to NH-0502.
- [x] nullable-of-any -> CONSISTENT with .NET `Comparer<T?>.Default` (null smallest: first ascending,
      last descending) on every provider. SQLite/SQL Server/MySQL match natively; PostgreSQL (NULLS LAST
      asc by default) is normalized by nORM's `OrderByTranslator` prepending a same-direction
      `(key IS NOT NULL)` rank key (flip-safe under Reverse), gated by the provider hook
      `RequiresExplicitNullOrderingForNullableKeys` (true only for Postgres). Proven by diagnostic dump
      (SQLite asc `2,4,3,5,1,6` / desc `6,1,5,3,2,4` == raw SQL == .NET). Pinned by
      `NullableOrderingContractTests` incl. the provider-hook matrix. CORRECT - no code change.
- [x] int / long / short / byte (signed + unsigned) -> int/long/short/sbyte/byte/uint order
      CONSISTENTLY with .NET's default comparer (all ranges fit SQLite's signed 64-bit INTEGER,
      including negatives, Min/Max, and uint above int.Max). Pinned by `IntegerOrderingContractTests`.
      The `ulong` boundary defect this cell surfaced (values > `long.MaxValue` silently stored negative,
      mis-ordered `4,5,1,2,3` vs .NET `1,2,3,4,5`, and `OverflowException` on read-back) was a write-path
      fidelity bug; RESOLVED under NH-0202 by a maintainer product decision (store as signed 64-bit -
      fully correct in [0, long.MaxValue], fail loud with `NormUsageException` above). Pinned by
      `UlongStorageContractTests`; the ordering half is now consistent with .NET across the full
      representable range.
- [x] decimal -> ordering matches .NET at FULL 28-digit precision on every provider. Magnitude
      (numeric not lexical), sign, zero, trailing-zero equality all correct; STORAGE and EXACT
      equality/GROUP BY/DISTINCT are full precision (canonical-text keys). The prior SQLite ORDER BY
      precision loss (CAST-AS-REAL sort key merged decimals agreeing to ~16 significant digits) was
      surfaced as a product decision; maintainer chose full precision. RESOLVED: `OrderByDecimalKeySql`
      hook - SQLite orders decimal via a registered `NORM_DECIMAL` collation (`decimal.Compare` on the
      canonical text), server providers keep native-DECIMAL ordering; arithmetic/aggregation keep the
      REAL coercion. Pinned by `DecimalOrderingContractTests` (incl. the 17th-digit case that CAST-REAL
      got wrong); full `Category=Fast` green (query-translation change).
- [x] double / float -> finite values and +/-Infinity order CONSISTENTLY with .NET (SQLite REAL,
      numeric ordering). Two DESIGN-EXCEPTIONs pinned (both identical to EF Core, same Microsoft.Data.Sqlite
      layer): NaN is not representable in SQLite REAL -> insert FAILS LOUD with
      `InvalidOperationException("Cannot store 'NaN' values.")` (no silent NULL), so NaN never enters an
      ordered set; -0.0 and +0.0 compare EQUAL in SQL and -0.0 loses its sign on read-back, whereas
      .NET `double.CompareTo` orders -0 < +0 (use a tiebreak for deterministic zero placement). Pinned by
      `DoubleOrderingContractTests`.
- [x] DateTime / DateTimeOffset -> CONSISTENT with .NET. Plain DateTime orders chronologically
      (fixed-width `yyyy-MM-dd HH:mm:ss` text + positional fractional suffix; correct across sub-second
      100ns diffs and the full Min/Max range; deliberately NOT numeric-coerced so MaxValue's .9999999
      doesn't overflow). DateTimeOffset orders by UTC INSTANT (normalized from the offset-suffixed text),
      not wall-clock, matching `DateTimeOffset.CompareTo`. Proven by diagnostic dump (DateTime `4,3,1,6,2,7,5`;
      DTO `2,4,1,3` = instant, NOT wall-clock `4,1,2,3`); boundary/sub-second round-trip exactly. Pinned by
      `DateTimeOrderingContractTests`.
- [x] TimeSpan / bool / enum / char -> all CONSISTENT with .NET's default comparer. TimeSpan (canonical
      'c' TEXT) is numeric-coerced so multi-day and negative durations order by value, not lexically
      (probe: nORM `3,7,4,5,6,2,1` vs lexical `7,3,4,5,6,1,2`). bool false<true. enum orders by UNDERLYING
      signed value incl. negative members (`-3` sorts first), not declaration order. char orders by code
      point (SQLite BINARY over UTF-8 preserves code-point order incl. non-ASCII BMP). Pinned by
      `ScalarTypeOrderingContractTests`.
- [x] converter-mapped column -> orders by the STORED PROVIDER value (ORDER BY is emitted against the
      stored column; ordering by the CLR key would need the inverse converter in SQL, impossible for an
      arbitrary C# ConvertFromProvider). DESIGN-EXCEPTION, identical to EF Core. Proven by diagnostic dump:
      order-preserving +1000 converter matches the CLR key (`2,4,1,3`); negating converter orders by
      stored -Score (`3,1,4,2`, NOT CLR `2,4,1,3`); enum->string orders ALPHABETICALLY by name
      (`1,2,4,3` High,Low,Low,Medium, NOT enum order `2,4,3,1`). Pinned by
      `ConverterColumnOrderingContractTests` (incl. read-back round-trip). **OrderBy operand row COMPLETE.**

Remaining operator rows (same method, to be enumerated to cell level):

- [x] aggregates (Sum/Avg/Min/Max/Count) x operand type -> null/empty/overflow edges PINNED
      (`AggregateEdgeSemanticsContractTests` + pre-existing `AggregateNullEmptySemanticsTests`):
      empty-nullable Sum=0/Min=null/Avg=null (CONSISTENT); Average(int) fractional 1.5 (CONSISTENT);
      Sum(int) out-of-range total fails loud with OverflowException (CONSISTENT observable);
      Sum(int) intermediate-overflow with in-range total returns the total - SQL wide-type accumulation,
      .NET checked accumulation throws (DESIGN-EXCEPTION, identical to EF Core, pinned both ways);
      Sum(long) overflow fails loud with the provider's 'integer overflow' error (DESIGN-EXCEPTION:
      provider-typed but loud, no silent wrap). The decimal-aggregate precision defect the probe isolated
      (Sum/Avg via CAST-AS-REAL lost precision beyond ~16 significant digits - Sum of the 17-digit pair
      = `2` vs .NET `2.00000000000000011`, 26-digit + 1 lost the +1 - and Min/Max could return a value
      that DOES NOT EXIST in the data) is RESOLVED under the established full-precision decimal policy:
      `DecimalAggregateSql` provider hook; SQLite routes SUM/AVG to registered `norm_decimal_sum`/
      `norm_decimal_avg` aggregates (decimal accumulation, canonical-text result) and MIN/MAX through the
      NORM_DECIMAL collation (true extreme, always an actual stored value) at ALL emit sites (top-level,
      windowed, grouped incl. CASE-filtered, nav-aggregate, explicit-scalar incl. DISTINCT); server
      providers keep native exact DECIMAL. Two missed decimal ORDER BY sites (projected-alias ordering,
      TakeLast/SkipLast key rebuild) also moved to `OrderByDecimalKeySql`. Pinned by
      `DecimalAggregatePrecisionContractTests`; full Category=Fast green (10965).
- [ ] GroupBy x key type x element selector x aggregate.
- [ ] Join / GroupJoin / SelectMany x key type (composite, converter, nullable).
- [ ] Distinct / set-ops x row shape (anonymous, converter).
- [ ] paging (Skip/Take/TakeLast/SkipLast) x ordering source.
- [ ] string predicates (Contains/StartsWith/EndsWith/==) x case/culture.
- [ ] window functions x partition/order key type.

## Parity audit

- [x] DESIGN-EXCEPTION: string ordering follows DB collation (matches EF Core). Not a defect;
      pinned + documented. This ticket introduces no new restriction.
- [x] CORRECT: Guid ordering on SQLite equals .NET `Guid.CompareTo` (canonical-hex TEXT storage +
      unsigned leading-field compare). Proven by diagnostic dump (storage=text, raw-SQL/nORM/.NET all
      `3,1,2,5,6,4`); pinned. SQL Server `uniqueidentifier` ordering divergence recorded for NH-0502.
- [x] CORRECT + DESIGN-EXCEPTION: double/float finite + +/-Infinity ordering matches .NET; NaN
      fails loud on store (SQLite REAL limit, matches EF Core - not silent corruption); -0.0 == +0.0 in
      SQL with sign lost on read-back. Proven by diagnostic dump; pinned. NaN/-0 write fidelity also
      recorded for the write-path matrix NH-0202.
- [x] CORRECT: nullable ordering matches .NET null-smallest semantics on all providers; Postgres
      NULLS-LAST default actively normalized via `RequiresExplicitNullOrderingForNullableKeys` +
      `(key IS NOT NULL)` rank key. Proven by dump + provider-hook matrix; pinned.

## Correctness-first audit

- [x] The string cell was proven by a differential probe (found the divergence), classified against
      EF Core behaviour, then pinned + documented rather than left silent.
- [ ] Remaining cells: each to be probed differentially before it is marked.

## Required verification

- The named gate (grows per cell) -> green.

## Verification evidence

- `StringOrderingContractTests` -> `Passed! Failed: 0, Passed: 1`. Pins DB-collation ordering
  (positive) and the intended divergence from .NET culture (negative).
- Differential probe (temporary, removed): nORM/SQLite `["Apple","B","Banana","a","apricot","c"]`
  vs .NET `["a","Apple","apricot","B","Banana","c"]` - confirmed real, then classified + pinned.
- `GuidOrderingContractTests` -> `Passed! Failed: 0, Passed: 2`. Diagnostic dump (temporary, removed):
  storage `typeof=text` canonical hex; raw SQL `ORDER BY Val`, nORM `OrderBy`, and .NET
  `Guid.CompareTo` all returned Ids `3,1,2,5,6,4` (incl. high-bit `ffff0000` sorting LAST) - proven
  consistent, then pinned (asc + desc) so a byte/BLOB-storage regression is caught.
- `DoubleOrderingContractTests` -> `Passed! Failed: 0, Passed: 4`. Diagnostic dump (temporary, removed):
  NaN insert threw `System.InvalidOperationException: Cannot store 'NaN' values.` (row absent);
  +/-Infinity stored `-Inf`/`Inf` and round-tripped; -0.0 stored/read as `0.0` (sign lost); nORM and
  .NET both ordered the storable values `2,3,4,5,6,7`. Pinned: finite+infinity ordering, NaN fail-loud
  + no-corruption, -0==+0 SQL equality with sign loss.
- `NullableOrderingContractTests` -> `Passed! Failed: 0, Passed: 4`. Diagnostic dump (temporary, removed):
  SQLite raw SQL, nORM, and .NET all ordered nullable ints asc `2,4,3,5,1,6` (nulls first) and desc
  `6,1,5,3,2,4` (nulls last). Provider-hook matrix asserted: base/SQLite/SQL Server/MySQL
  `RequiresExplicitNullOrderingForNullableKeys = false`, PostgreSQL `= true`.
- `IntegerOrderingContractTests` -> `Passed! Failed: 0, Passed: 6` (int/long/short/byte/sbyte/uint).
  Diagnostic dump (temporary, removed) isolated the ulong defect: storing `(ulong)long.MaxValue+1`
  quoted as `-9223372036854775808`, `ulong.MaxValue` as `-1`; nORM/SQLite ordered `4,5,1,2,3` vs .NET
  `1,2,3,4,5`; read-back threw `OverflowException: Value was either too large or too small for a UInt64`.
