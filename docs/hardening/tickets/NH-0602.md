# NH-0602 - Temporal versioning per-cell matrix (deep)

Ticket: NH-0602
Status: Verified
Owner: unassigned
Area: temporal/asof-fidelity
Priority: P1
Quarantine allowed: no
Named gate: dotnet test tests/ --filter "FullyQualifiedName~TemporalAsOfBoundary|FullyQualifiedName~TemporalHistoryValueFidelityContractTests|FullyQualifiedName~TemporalHistoryReconstructionFuzzTests|FullyQualifiedName~FastPathQueryTrackingContractTests" (extended per cell as cells close)

## Problem

The temporal campaign fixed sub-second version collapse (millisecond triggers) and AsOf plan-cache
rebinding, and the reconstruction fuzzer replays random add/update/delete chains through AsOf at
checkpoints BETWEEN versions. What the fuzzer deliberately does not reach: the EXACT-boundary AsOf
contract (a timestamp equal to a version's start - inclusive or exclusive is unpinned), history-row
fidelity over BOUNDARY values (it uses small ints/strings), and the interplay of a migration
recreate with a temporal table's history and triggers. This matrix closes those cells with the
established differential method.

## Non-negotiable scope

Allowed paths:

- tests/**
- src/nORM/Versioning/**
- src/nORM/Providers/**
- src/nORM/Query/**
- docs/hardening/tickets/NH-0602.md
- docs/v1-sharpening/temporal-versioning.md

Acceptance criteria:

`[x]` closed, `[~]` partial/live-pending, `[ ]` open.

- [x] AsOf BOUNDARY semantics -> CORRECT and now PINNED: intervals are HALF-OPEN
      (`ts >= __ValidFrom AND ts < __ValidTo`), so a timestamp exactly equal to a transition instant
      returns the NEW version - identical to SQL Server FOR SYSTEM_TIME AS OF. Probed against the
      ACTUAL stored trigger timestamps parsed back from the history table (so bound-parameter text
      vs stored millisecond text compares correctly at the boundary): exact-boundary=V2,
      boundary-1ms=V1, boundary+1ms=V2, exact-first-From=V1 (inclusive), before-first=EMPTY,
      after-last=current. Side-fact learned: the history table holds ALL versions incl. the current
      one (open-ended ValidTo '9999-12-31'). Pinned by `TemporalAsOfBoundaryContractTests`. No code
      change.
- [x] history-row VALUE fidelity -> CORRECT: after an update, the v1 history row holds the OLD
      boundary values byte-exact (raw SELECT located it by its exact 17-digit decimal text; the
      offset-suffixed DTO text, astral string, and blob all verbatim) while the live row holds the
      new set; AsOf between versions reconstructs v1 exactly through the materializer (decimal
      precision, DTO offset, astral pair, blob bytes). Pinned by
      `TemporalHistoryValueFidelityContractTests`. No code change.
- [x] update/delete CHAIN reconstruction -> ALREADY GENUINELY COVERED by
      `TemporalHistoryReconstructionFuzzTests` (random add/update/delete chains incl. key reuse,
      checkpoint reconstruction, AsOf-before-all empty) and its live-provider variant - cited as
      evidence; value domain there is int/string (boundary values covered by the fidelity cell).
- [x] temporal DELETE final-state -> CORRECT: after deleting the boundary-value row, the current
      query is empty and AsOf before the delete returns the final (v2) values byte-exact. Pinned by
      `TemporalHistoryValueFidelityContractTests`. No code change.
- [x] temporal + MIGRATION interplay -> PROBED; TWO real defects found (differential probe over an
      EnableTemporalVersioning table + SchemaDiffer/SqliteMigrationSqlGenerator DROP-COLUMN recreate):
      (1) CONFIRMED DEBT, fix decided: the recreate silently DROPS the three versioning triggers
      (SQLite recreate semantics) - post-migration updates write NO history until some future
      context re-runs the temporal bootstrap, so a version is PERMANENTLY missing from the chain
      (probe observed V=1 -> V=2 -> V=4 with V=3 unrecorded). AsOf for pre-migration timestamps
      keeps working; the bootstrap's unconditional CREATE TRIGGER IF NOT EXISTS restores the
      triggers on the next context. Deeper (all trigger-emulated providers): ADD COLUMN never
      extends the history table nor regenerates the column-list triggers, so a new column's values
      are silently absent from history forever. Product decision (user): FULL temporal-aware
      migrations - history schema mirrored in lock-step + triggers re-emitted from the new schema
      by every migration generator. Tracked and LANDED as ticket NH-0612: SQLite closed
      behaviourally (`TemporalMigrationContractTests` - the very next write after a recreate
      migration is versioned, no missing version; AsOf spans the migration; added-column values
      reach history from the first write; Down restores shape and versioning; tenant predicate
      preserved in regenerated triggers), and all three server generators closed at the SQL-text
      level (`ServerTemporalMigrationDdlContractTests`). This cell's SQLite-closable debt is
      RESOLVED; the server behavioural interplay rides NH-0612's live-pending cell.
      (2) FIXED HERE (in scope, spinoff discovery): the probe's "restored triggers still record no
      history" mystery was NOT temporal at all - the fast-path query executor materialized entities
      WITHOUT change tracking. Every fast-path list shape (simple property-equality Where,
      plain Take, filtered/ordered page) returned entities invisible to SaveChanges: load-by-key,
      mutate, SaveChanges silently affected 0 rows (silent update loss on the most common query
      shape). Fixed by mirroring QueryExecutor.ProcessEntity in the fast path (identity-map
      canonical instance, Unchanged snapshot, lazy-loading wiring) with AsNoTracking and
      NoTracking-context opt-outs honored. Pinned by `FastPathQueryTrackingContractTests`
      (8 tests incl. the end-to-end persist round-trip and identity-map canonicalization).
- [x] LIVE temporal parity -> CLOSED with genuine, NON-VACUOUS live evidence (2026-07-16, after
      repointing NORM_TEST_* at the normtest application database - the earlier 1867/1867 run had
      self-skipped these suites on provider-owned databases, recorded as an honesty correction):
      LiveProviderTemporal parity/reconstruction + the new TemporalMigrationLiveBehaviourTests,
      26/26 GREEN on live SqlServer + PostgreSQL + MySQL. Original: the standing
      LiveProviderTemporal* suites are the gate; re-run pending.
- [x] EAGER-LOAD x AsOf (post-Verified adversarial cell, 2026-07-16) -> IMPLEMENTATION-DEBT
      FOUND AND FIXED: Include under AsOf reconstructed the ROOT through the history window
      but read every eager-loaded relation from the LIVE tables - historical roots came back
      with post-timestamp child values and post-timestamp children (probe: AsOf(t1) truth
      one child Val=10; returned Val=99 plus a child added after t1). Fix: QueryPlan carries
      the AsOf timestamp (safe to bake: AsOf() embeds it as an expression CONSTANT, so the
      plan-cache fingerprint hashes the full value and each timestamp owns its plan) and
      IncludeProcessor reads every include level - including nested EXISTS levels - through
      the same history-window derived table (or provider-native AS OF clause) with the
      timestamp bound once per command. Many-to-many + AsOf now fails loud AT TRANSLATION:
      the association table is raw and unversioned, so era membership is unknowable
      (DESIGN-EXCEPTION boundary; versioning association tables is a future product
      decision). Pinned by IncludeAsOfConsistencyContractTests +
      ManyToManyTemporalContractTests; TemporalAsOfPlanCacheTests unchanged.

## Completion note

All five SQLite-closable cells are CLOSED (AsOf boundary, history value fidelity, chain, delete
final-state, migration interplay via NH-0612). The remaining `[~]` cell requires live-provider
runs (provider-native temporal parity) and is deliberately not closable from SQLite-only
evidence; the ticket stays In Progress until those live suites execute. No SQLite-side work
remains.

## Parity audit

- [x] This ticket introduces no new restriction; it pins AsOf boundary and history-fidelity
      contracts.

## Correctness-first audit

- [x] Each SQLite-closable cell probed differentially before it is marked. (Satisfied: every closed cell above cites its differential probe/contract test.)

## Required verification

- The named gate (grows per cell) -> green.

## Verification evidence

- (accumulates per cell)
