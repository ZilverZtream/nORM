# Fuzzer dry-window log

Chronological, dated evidence toward the RELEASE.md "fuzzers dry for a sustained window" bar.
Every entry is a REAL run on the stated tree; kills are recorded as loudly as dry runs.

| Date (UTC) | Fuzzer | Range / scope | Result |
| --- | --- | --- | --- |
| 2026-07-16 | CacheStalenessFuzzTests (EXTENDED: +join, +window shapes) | seeds 0-119 x 5 shapes | DRY |
| 2026-07-16 | LiveProvider full suite (all 3 servers) | 1867 tests | GREEN |
| 2026-07-16 | Temporal live (parity + reconstruction + migration behavioural, normtest) | 26 tests | GREEN |
| 2026-07-16/17 | LinqParityFuzzTests (env-directed sweep, full shape battery ~1300 cases/seed) | seeds 502000-513399 (11400) | DRY |
| 2026-07-16 | CrudStateMachineFuzzTests (env-directed sweep, all 3 machines/seed) | seeds 602000-602749 (750) | DRY |
| 2026-07-16 | **CrudStateMachineFuzzTests KILL at seed 602775** (relationship machine): `Remove()` threw "Entity graph exceeds maximum depth of 10" on a LEGAL re-parenting chain of distinct instances (stale collection membership fixup deliberately leaves behind) — the validator's fixed depth cap was a false-positive tripwire; termination was already guaranteed by the visited set. Cap removed, killing seed pinned as InlineData + `DeepEntityGraphValidationContractTests`. Range 602750-602899 re-swept clean post-fix. | seed 602775 | KILL -> FIXED |
| 2026-07-16 | **Adversarial-probe KILL (Include x AsOf era mixing)**: eager loads under AsOf read the LIVE tables while the root reconstructed history — historical roots returned post-timestamp child values/rows. Fixed: the plan carries the AsOf timestamp and every include level reads through the same history window; M2M + AsOf fails loud (association tables unversioned). Pinned `IncludeAsOfConsistencyContractTests` + `ManyToManyTemporalContractTests`. | adversarial probe | KILL -> FIXED |
| 2026-07-16 | **Adversarial-probe KILL (owned collections empty on untracked reads)**: owned loading was gated on the tracking mapping — AsNoTracking/NoTracking-default/AsOf reads returned owners with silently EMPTY `OwnsMany` collections; one materialize path never loaded owned rows at all. Fixed (owned-load mapping resolved independent of tracking, all three paths) + `OwnsMany(tableName:)` now reaches the owned type's mapping (temporal bootstrap crashed on the CLR-name table). AsOf over owners with owned collections fails loud until owned history carries the FK (follow-up open item). Pinned `OwnedCollectionUntrackedAndAsOfContractTests`. | adversarial probe | KILL -> FIXED |
| 2026-07-16/17 | CrudStateMachineFuzzTests (post-fix re-sweep + batches 7-14) | seeds 602750-610549 (7800) | DRY |
| 2026-07-16/17 | TemporalHistoryReconstructionFuzzTests (env-directed sweep, 10 rounds/seed) | seeds 702000-702639 (640) | DRY |
| 2026-07-16/17 | BulkCudOracleFuzzTests (env-directed sweep, 150 steps/seed) | seeds 802000-830499 (28500) | DRY |
| 2026-07-16/17 | SqliteMigrationDataPreservationFuzzTests (env-directed sweep, 60 cases/seed) | seeds 902000-919099 (17100) | DRY |
| 2026-07-16/17 | CacheStalenessFuzzTests (env-directed sweep, 5 shapes/seed) | seeds 1002000-1018799 (16800) | DRY |
| 2026-07-16/17 | MixedTokenOccFuzzTests (env-directed sweep, 3-context interleaving/seed) | seeds 1102000-1110399 (8400) | DRY |
| 2026-07-16/17 | RetryFaultInjectionFuzzTests (env-directed sweep, fault machine/seed) | seeds 1202000-1210399 (8400) | DRY |
| 2026-07-17 | LiveProviderScaffold* suites (all 3 servers, schema introspection + model generation + compile parity) | 734 tests, 0 skipped | GREEN |
| 2026-07-17 | **Adversarial-probe KILL (join shapes x AsOf era mixing)**: every JOIN-based shape leaked live state under AsOf — nav-scalar projections era-mixed (`e1\|d1x`), nav predicates evaluated against LIVE principals (silent row loss), SelectMany ignored AsOf entirely, correlated nav aggregates counted LIVE membership. Root cause: only the root FROM substituted the history window. Fixed via an ambient temporal table-source scope (mirrors the referenced-table ambient) consulted at EVERY mapped-table emission site — SelectMany joins, JoinBuilder, aggregate/grouped subqueries, ETSV + SCV nav emitters; mixed AsOf timestamps and ExecuteUpdate/Delete-over-AsOf fail loud. Pinned `JoinAsOfConsistencyContractTests` (7 probes) + `TemporalAsOfNavigationTests` rewritten to the era-consistent contract. | adversarial probe | KILL -> FIXED |
| 2026-07-17 | LiveProviderTemporalReconstructionFuzzTests (EXTENDED: +relational machine on live SqlServer + Postgres + MySQL — history-window derived tables proven in JOIN/subquery positions per dialect) | 6 tests (2 machines x 3 servers), 0 skipped | GREEN |
| 2026-07-17 | TemporalHistoryReconstructionFuzzTests (EXTENDED: +relational machine — randomly mutated principal/dependent graph replayed per checkpoint through nav projection, nav predicate, SelectMany, correlated aggregate, Include, Union, and Contains, each against the oracle state; the shape coverage the single-table machine lacked when KILLs 44/46/47 were found by hand) | seeds 702640-705139 (2500) x 2 machines | DRY |
| 2026-07-17 | **Adversarial-probe KILL (tenant isolation x explicit join shapes — LIVE, follow-up on the nav-subquery kill)**: GroupJoin's grouped inner rows and the GroupJoin+DefaultIfEmpty LEFT-JOIN flatten DROPPED the inner source's injected tenant/soft-delete Where (the root rewrite wraps Join/GroupJoin inners, but only HandleInnerJoin pushed inner WHERE conditions into the JOIN ON); cross-join SelectMany's inner filter renderer used global filters WITHOUT tenant (the inner sits inside the collection-selector lambda the top-level rewrite never reaches). Fixed: GroupJoin + LeftJoins flatten now push ExtractInnerWhereConditions into the LEFT JOIN ON (filtered inner rows read as UNMATCHED, outer rows survive), and RenderSelectManyInnerGlobalFilterSql uses CombineWithTenant. Plain INNER Join was already correct. Pinned in `TenantJoinAsOfConsistencyContractTests` (explicit Join, GroupJoin count, left-join flatten, cross-SelectMany). | adversarial probe | KILL -> FIXED |
| 2026-07-17 | **Adversarial-probe KILL (tenant isolation x navigation subqueries — LIVE, not AsOf-specific)**: correlated navigation aggregates leaked ACROSS TENANTS — a foreign tenant's dependent row sharing a foreign-key value folded into `d.Emps.Count()` (probe: count 2 where tenant truth is 1). Scope on inspection: the nav Count/Any/All emitter applied global filters but NOT the tenant predicate (a separate mechanism); the nav Sum/Min/Max/Avg emitters (projection AND predicate side) applied NO visibility filters at all (soft-delete included); the two-hop count applied none; and a FORGED FK (another tenant's dependent pointing at this tenant's principal key) exposed principal VALUES cross-tenant through nav-scalar reads. Fixed via `GlobalFilterFragment.CombineWithTenant` (global filters AND tenant, skip-if-no-tenant-column) applied at every navigation-subquery emitter — count, scalar aggregates both sides, both hops of two-hop, and all principal-read paths (cross-tenant principal now reads as MISSING). ExecuteUpdate's nav-aggregate already tenant-scoped (prior art). Pinned `TenantJoinAsOfConsistencyContractTests` (windowed shapes under AsOf + live aggregates + forged-FK probes). | adversarial probe | KILL -> FIXED |
| 2026-07-17 | **Adversarial-probe KILL x3 (composite shapes x AsOf, follow-up round)**: (1) set-operation arms leaked — the lazy scope opened only when the AsOf node was VISITED, so a Union's other arm (translated before/after the scope's life) read LIVE, order-dependently; (2) Contains-over-mapped-query inner sources never windowed (nested sub-plan gated on its own null timestamp); (3) the SQL was fixable while results still leaked — a set-op arm's AsOf set no-tracking only on the SUB-context, so the outer plan TRACKED and the materializer aliased historical rows to already-tracked live entities (silent current-state return). Fixed: a pre-scan opens the window at the OUTERMOST translation entry (position-independent), the root-FROM substitution gates on the ambient (nested arms window), scope ownership rides the referenced-table scope's outermost signal (pool-fresh nested translators must not end it), and the pre-scan stamps AsOfTimestamp + no-tracking on the outermost plan. Tag resolution cached per translation (two resolves could straddle a tag rewrite). Pinned `SubqueryAsOfConsistencyContractTests` (set ops, GroupBy aggregates, grouped scalar aggregates, Contains subqueries). | adversarial probe | KILL -> FIXED |
| 2026-07-17 | TenantIsolationRelationalFuzzTests (NEW 9th family, NORM_TENANT_FUZZ_SWEEP: two tenant contexts mutate a shared relational graph incl. FORGED cross-tenant FKs; every round replays nav projection, correlated Count/Sum, SelectMany, explicit Join, GroupJoin count, and predicate aggregates under BOTH tenants vs per-tenant oracles - the sweepable coverage for the KILL 48/49 leak class) | seeds 1302000-1313999 (12000) + 3 fixed | DRY |
